#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    if matrix is None:
        return(None)
    if not matrix:
        return(None)
    else:
        for i in range(matrix):
            for j in i:
    return(newmatrix)
