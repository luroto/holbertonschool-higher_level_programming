#!/usr/bin/python3
""" This module checks if some object is an instance on some
way of a class"""


def is_kind_of_class(obj, a_class):
    """this function do the check"""
    return(isinstance(obj, a_class))
